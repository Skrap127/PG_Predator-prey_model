\chapter[Modelo de Leslie-Gower]{El modelo de Leslie-Gower con respuesta funcional sigmoidea}

En este capítulo se analiza el modelo presa-depredador de tipo Leslie-Gower propuesto por González-Olivares et al. (2015)\cite{articulo}, en el cual la población de presas exhibe una respuesta funcional de Holling tipo III, caracterizada por su forma sigmoidea.

\section{Descripción del modelo}

El modelo se describe mediante el siguiente sistema autónomo de ecuaciones diferenciales ordinarias de tipo Kolmogorov:

\begin{equation}
	X_{\mu} =
	\begin{cases}
		\dfrac{dx}{dt} = \left(r \left(1 - \dfrac{x}{K}\right) - \dfrac{q x y}{x^2 + a^2}\right) x, \\
		\dfrac{dy}{dt} = s \left(1 - \dfrac{y}{n x}\right) y,
	\end{cases}
	\label{eq:modelo1}
\end{equation}

donde $x(t)$ y $y(t)$ representan las densidades poblacionales de presas y depredadores, respectivamente, para $t \geq 0$. Los parámetros $\mu = (r, a, s, K, n, q) \in \R^6_{+}$ son estrictamente positivos y tienen el siguiente significado biológico:

\begin{itemize}
	\item $r$: tasa de crecimiento intrínseco de las presas;
	\item $K$: capacidad de carga ambiental de las presas;
	\item $q$: tasa máxima de consumo per cápita de los depredadores (tasa de saciedad);
	\item $a$: cantidad de presas necesarias para alcanzar la mitad de $q$ (media de la tasa de saturación);
	\item $s$: tasa de crecimiento intrínseco de los depredadores;
	\item $n$: medida de la calidad alimenticia, que indica la eficiencia de conversión de presas consumidas en nuevos depredadores.
\end{itemize}

El sistema \eqref{eq:modelo1} no está definido en $x=0$ (eje $y$), por lo que el dominio biológicamente relevante es el conjunto $\Omega = \{ (x,y) \in \R^2 \mid x > 0, \; y \geq 0 \}$.

\section{Análisis}

Dado un sistema $\dot{X} = f(X)$ con $X = (x,y)$, los puntos críticos se obtienen resolviendo $f(X) = 0$. Para el sistema \eqref{eq:modelo1}, esto es:

\begin{equation}
	\begin{cases}
		\left(r \left(1 - \dfrac{x}{K}\right) - \dfrac{q x y}{x^2 + a^2}\right) x = 0, \\
		s \left(1 - \dfrac{y}{n x}\right) y = 0.
	\end{cases}
	\label{eq:sistema-eq}
\end{equation}

De la segunda ecuación, $y = 0$ o $y = n x$. Para $y=0$, se obtiene $x = K$, por tanto $(K,0)$ es un punto crítico. De las intersecciones de las isoclinas $$y = n x, \; \; \; \; \; \; y = \dfrac{r}{q x} \left(1 - \dfrac{x}{K}\right) (x^2 + a^2),$$

se obtinene los demás puntos críticos del sistema, donde se verificará más adelante que solo existe una solución $(x_e, y_e) \in \R^2$,  el cual existe en el primer cuadrante si y solo si $x_e < K$.
% Ctrl + T (comentar); Ctrl + U (descomentar)
%El punto $(K,0)$ es un punto de silla. Considerando $f_1(x,y) = r x - \frac{r x^2}{K} - \frac{q x^2 y}{x^2 + a^2}$ y $f_2(x,y) = s y - \frac{s y^2}{n x}$, la matriz jacobiana es:
%
%\begin{equation}
%	Df(X) =
%	\begin{bmatrix}
%		r - \dfrac{2 r x}{K} - \dfrac{(2 q x y)(x^2 + a^2) - 2 x (q x^2 y)}{(x^2 + a^2)^2} & -\dfrac{q x^2}{x^2 + a^2} \\
%		-\dfrac{s y^2}{n x^2} & s - \dfrac{2 s y}{n x}
%	\end{bmatrix}.
%	\label{eq:jacobiana}
%\end{equation}
%
%En $(K,0)$:
%
%\begin{equation}
%	Df(K,0) =
%	\begin{bmatrix}
%		-r & -\dfrac{q K^2}{K^2 + a^2} \\
%		0 & s
%	\end{bmatrix}.
%	\label{eq:jacobiana-K0}
%\end{equation}
%
%Los valores propios se hallan del polinomio característico $\lambda^2 - \operatorname{tr}(A) \lambda + \det(A) = 0$, con $A = Df(K,0)$:
%
%\begin{equation}
%	\lambda^2 - (s - r) \lambda - r s = 0.
%	\label{eq:car-K0}
%\end{equation}
%
%Por la fórmula cuadrática:
%
%\begin{align}
%	\lambda &= \frac{s - r \pm \sqrt{(s - r)^2 + 4 r s}}{2} = \frac{s - r \pm (r + s)}{2},
%	\label{eq:valores-propios}
%\end{align}
%
%obteniendo $\lambda_1 = s > 0$ y $\lambda_2 = -r < 0$. Dado que tienen signos opuestos, $(K,0)$ es un punto silla.

Con el propósito de analizar el comportamiento cualitativo del sistema y simplificar los cálculos algebraicos, introducimos el cambio de variables $x = K u$ y $y = n K v$, la cual define una transformacion lineal y por ende, un difeomorfismo entre el sistema \eqref{eq:modelo1} y el resultante:
\begin{equation*}
	\begin{cases}
		\dfrac{du}{dt} = r\left((1-u) - \dfrac{qn}{r} \dfrac{uv}{u^{2}+\left(\dfrac{a}{K}\right)^{2}}\right)u \\
		\dfrac{dv}{dt} = s \left(1-\dfrac{v}{u}\right)v
	\end{cases}.
\end{equation*}
Posteriormente, se realiza un reescalado de la variable temporal mediante la relación diferencial $d\tau = r/[u(u^2+A^2)]dt $, donde $A = a/K$. Dado que el factor de escala $r/[u(u^2+A^2)]$ es una función suave y positiva en el conjunto de definición $\bar{\Omega} = \{ (u, v) \in \R^{2} \; | \; u > 0, \; v \geq 0 \}$, la dirección de las trayectorias se preserva \cite{chicone2024}[p.~14](Colocar proposicion 1.14 en preli), por lo que el sistema resultante al aplicar el reescalado será topológicamente equivalente, este es:
\begin{equation*}
	\begin{cases}
		\dfrac{du}{d\tau} = \left((1-u)\left(u^{2}+\left(\dfrac{a}{K}\right)^{2}\right) - \dfrac{qn}{r} uv\right)u^{2} \\
		\dfrac{dv}{d\tau} = \dfrac{s}{r} \left(u-v\right)\left(u^{2}+\left(\dfrac{a}{K}\right)^{2}\right)v
	\end{cases}
\end{equation*}
Esto da paso a la siguiente proposición.

\begin{pro}
	El sistema \eqref{eq:modelo1} es topológicamente equivalente al sistema
	\begin{equation}
		Y_{\eta}:
		\begin{cases}
			\dfrac{du}{d \tau} = ((1 - u)(u^{2} + A^{2}) - Q u v)u^{2}\\
			\dfrac{dv}{d \tau} = B(u - v)(u^{2} + A^{2})v
		\end{cases},
		\label{eq: modelo2}
	\end{equation}
	donde, $\eta = (A, Q, B) \in (0, 1) \times \R^{2}_{+}$ con $A = \dfrac{a}{K}, \; Q = \dfrac{qn}{r}, \; B = \dfrac{s}{r}$.
\end{pro}

\begin{proof}
	La transformación $\varphi : \bar{\Omega} \times \R \rightarrow \Omega \times \R$, dada por
	\begin{equation*}
		\varphi(u, v, \tau) = \left( Ku, \; nKv, \; \dfrac{u\left(u^{2}+\left(\dfrac{a}{K}\right)^{2}\right)}{r} \tau \right) = (x, y, t),
	\end{equation*} define una correspondencia biunívoca y suave entre el sistema \eqref{eq:modelo1} y el \eqref{eq: modelo2}, por lo que $\varphi$ es un difeomorfismo suave, así, $Y_{\eta} = \varphi \circ X_{\mu}$ es topológicamente equivalente al sistema \eqref{eq:modelo1}, donde $Y_{\eta}$ define un sistema polinómico de grado 5.
\end{proof}

\begin{remark}
	Dado que $a$ representa un umbral ecológico inferior a la capacidad de carga $K$ de las presas, se asume $0 < a < K$, lo que implica $0 < A < 1$.
\end{remark}

%\begin{remark}
%	La transformación $\varphi : \bar{\Omega} \times \R \rightarrow \Omega \times \R$, dada por
%	\begin{equation*}
%		\varphi(u, v, \tau) = \left( Ku, \; nKv, \; \dfrac{u\left(u^{2}+\left(\dfrac{a}{K}\right)^{2}\right)}{r} \tau \right) = (x, y, t),
%	\end{equation*} define una correspondencia biunívoca y suave entre el sistema \eqref{eq:modelo1} y el \eqref{eq: modelo2}, por lo que $\varphi$ es un difeomorfismo suave. Su matriz jacobiana
%	\begin{equation}
%		D\varphi(u, v, \tau) =
%		\begin{bmatrix}
%			K & 0 & 0 \\
%			0 & nK & 0 \\
%			\dfrac{3u^{2}+\left(\dfrac{a}{K}\right)^{2}}{r} & 0 & \dfrac{u\left(u^{2}+\left(\dfrac{a}{K}\right)^{2}\right)}{r}
%		\end{bmatrix},
%	\end{equation}
%	tiene $\det D\varphi = nK^{2} \dfrac{u\left(u^{2}+\left(\dfrac{a}{K}\right)^{2}\right)}{r} > 0$ para $u > 0$, por lo que preserva la orientación temporal \cite{articulo}. Así, $Y_{\eta} = \varphi \circ X_{\mu}$ es topológicamente equivalente al sistema \eqref{eq:modelo1}, donde $Y_{\eta}$ define un sistema polinómico de grado 5 \cite{articulo}.
%\end{remark}
\begin{remark}
	Desde la perspectiva de la teoría cualitativa, resulta conveniente representar el sistema diferencial autónomo \eqref{eq: modelo2} mediante su campo vectorial asociado $Y_{\eta} = P(u,v) \partial_u + Q(u,v) \partial_v$, donde las componentes del campo están definidas por: 
	\begin{equation*}
		\begin{aligned}
			P(u, v) &= ((1 - u)(u^{2} + A^{2}) - Q u v)u^{2} \; \; \; \text{y} \; \; \;
			Q(u, v) &= B(u - v)(u^{2} + A^{2})v
		\end{aligned}.
	\end{equation*}
	Esta formalización se fundamenta en que si una población se encuentra en la posición $(u,v)$ en un tiempo $\tau$, el sistema \eqref{eq: modelo2} asigna un único vector tangente $(P(u, v), Q(u, v))$, permitiendo así interpretar las soluciones como curvas integrales cuya geometría es dictada por la estructura del operador diferencial $Y_{\eta}$. Otra forma de expresar el campo vectorial es escribiéndolo como $X = (P(u, v), Q(u, v))$.
\end{remark}
\begin{remark}
    El sistema \eqref{eq: modelo2} está definido en $$\tilde{\Omega} = \{ (u, v) \in \R^{2} \; | \; u \geq 0, \; v \geq 0 \}.$$
    Recordemos que el modelo original \eqref{eq:modelo1}, no está definido en el eje $y$ y en particular en $(0,0)$ debido a la forma de la respuesta funcional Leslie-Gower. Al expresar el sistema como un campo polinomial de quinto orden, los autores logran una extensión topológicamente equivalente que sí es válida en el origen. Esto permite estudiar la extinción simultánea de ambas especies, algo que el modelo original impedía analíticamente.
\end{remark}
Del cambio de variable $x = Ku$, podemos deducir que en un sentido biológico el máximo valor que puede tomar $u$ es $1$, debido a que $K$ representa el umbral máximo de presas que puede soportar el ambiente. Además como $y = nx$ representa la capacidad de carga del depredador, el máximo valor que puede tomar $y$ es $nK$, así del cambio de variable $y = n K v$, el máximo valor que puede tomar $v$ es también $1$. Por lo que sólo tiene sentido biológico analizar la siguiente región. % Suponiendo que $u \in [0, 1]$, se puede evidenciar de la segunda ecuación del sistema \eqref{eq: modelo2}; que, para $v > 1$ el flujo se dirige hacia abajo en todos estos casos. Esto da paso al siguiente lema.
\begin{lem}
	El conjunto $\Gamma = \{ (u, v) \in \R^{2} \; | \; 0 \leq u \leq 1, \; 0 \leq v \leq 1 \} \subset \tilde{\Omega}$ es una región positivamente invariante.
\end{lem}
\begin{proof}
	Del sistema \eqref{eq: modelo2} deducimos que 
	
	\begin{itemize}
		\item para $u = 0$, $\dot{u} = 0$ y $\dot{v} = -A^2Bv^2 \leq 0$; es decir, el flujo se mantiene sobre este eje y además decrece para el caso $v\neq0$. En el vértice $(0, 0)$ el campo se reduce a $(0,0)$, esto indica que cualquier trayectoria que comience exactamente aquí, se mantendrá en este punto.
		
		\item para $v = 0$, $\dot{u} = (1 - u)(u^{2} + A^{2})u^{2} \geq 0$ (puesto que $u \in [0, 1]$) y $\dot{v} = 0$; así, al igual que en el caso $u=0$, el flujo se mantiene sobre este eje; es decir, los ejes coordenados son conjuntos invariantes. En el vértice $(1, 0)$ el campo se reduce a $(0,0)$, así el flujo no ''sale" de la región propuesta.
		
		\item para $u=1$, $\dot{u} = -Qv \leq 0$ y $\dot{v} = B(1 - v)(1 + A^{2})v \geq 0$. Donde, $\dot{u} = 0$, solo si $v=0$; este sería el caso del vértice $(1,0)$ anteriormente analizado. Así para $v>0$, $\dot{u} < 0$ y $\dot{v} > 0$ por lo que el flujo sobre la frontera derecha, se dirige hacia la izquierda y hacia arriba. 
		
		\item para $v=1$, tenemos que $\dot{v}=B(u-1)(u^2+A^2)$. Dado que $0\leq u \leq1$, el factor $(u-1)$ es no positivo, lo que implica $\dot{v} \leq0$. Específicamente, $\dot{v} =0$ si y solo si $u=1$. En el vértice $(1,1)$, el campo vectorial se reduce a $(-Q,0)$, lo que indica un flujo puramente horizontal hacia la izquierda, manteniendo así el flujo del campo sobre el borde superior o entrando al interior de la región $\Gamma$ en el caso $u<1$.
	\end{itemize}
	
	Por lo tanto, el cuadrado unitario $\Gamma$ es una región positivamente invariante.
\end{proof}
\begin{remark}
	Es claro que el conjunto $\Gamma \subset \R^2$ es conexo y cerrado, por lo que $\Gamma$ es una región de atrapamiento. Además, al ser $\Gamma$ un conjunto acotado, da paso al siguiente corolario.
	\label{re:atrapamiento}
\end{remark}
\begin{cor}
	Las soluciones del sistema \eqref{eq: modelo2} son acotadas.
\end{cor}
\begin{proof}
	proof.
\end{proof}
Los puntos críticos de \eqref{eq: modelo2} se obtienen resolviendo $g(Y) = 0$, donde $\dot{Y} = g(Y)$ con $Y = (u,v)$, es decir:
\begin{equation}
	\begin{cases}
		((1 - u)(u^{2} + A^{2}) - Q u v)u^{2} = 0\\
		B(u - v)(u^{2} + A^{2})v = 0
	\end{cases}.
\end{equation}
De la primera: $u=0$ o
\begin{equation}
	v = \dfrac{(1-u)(u^2 + A^2)}{Q u} ;
	\label{eq:isoclina-presa_modelo2}
\end{equation}
de la segunda: $v=0$ o $v=u$. Así, $(0,0)$ y $(1,0)$ son equilibrios; el equilibrio $(u_e, v_e)$ surge de la intersección de las isoclinas \eqref{eq:isoclina-presa_modelo2} y $v = u$ \cite{articulo}, esto es equivalente a hallar las raíces del polinomio de grado 3:
\begin{equation}
	P(u) = Qu^{2} - (1-u)(u^2 + A^2) = u^{3} + (Q-1)u^{2} + A^{2}u - A^{2}.
	\label{eq:polinomioIsoclinasModelo2}
\end{equation}
Debido a la naturaleza de nuestro problema, solo son de interés las raíces reales positivas de $P$. Para determinar la cantidad de raíces reales (positivas y negativas), haremos uso de la regla del signo de Decartes \cite{murray2002}[p.~509] y nos apoyaremos en el Teorema fundamental del álgebra.

\begin{pro}
	El polinomio \eqref{eq:polinomioIsoclinasModelo2} tiene una única raíz real positiva.
	\label{pro:exitenciaH}
\end{pro}
\begin{proof}
	Según la naturaleza del coeficiente del segundo termino del polinomio \eqref{eq:polinomioIsoclinasModelo2}, obtenemos los siguientes casos: 
	\begin{itemize}
		\item Si $Q-1 \geq 0, \; P(u)$ tiene 1 cambio de signo, y $$P(-u)=-u^{3}+(Q-1)u^{2}-A^{2}u-A^{2}$$ tiene 2 cambios de signo. Por lo tanto, $P(u)$ tiene una raíz real positiva y dos negativas, o una raíz real positiva y dos complejas.
		\item Si $Q-1<0, \; P(u)$ tiene 3 cambios de signo y $P(-u)$ tiene 0 cambios de signo. Por lo tanto, $P(u)$ tiene tres raíces reales positivas, o una sola raíz real positiva y dos complejas.
	\end{itemize}
	En todo caso, se garantiza la existencia de al menos una raíz real positiva, además, nótese que: $$P(0) = -A^{2}<0 \; \text{ y } \; P(1) = Q>0,$$ así por el teorema del valor intermedio, esta raíz $H \in (0,1)$, esto también se puede deducir de la isoclina \eqref{eq:isoclina-presa_modelo2}. Descartemos ahora la posible existencia de tres raíces reales positivas simultáneamente; para ello observemos que el cociente entre $P(u)$ y el monomio $u - H$ es $$P_{1}(u) = u^{2}+(Q-1+H)u+(Q-1+H)H+A^{2}$$ con resto $$R(u) = H^{3}+(Q-1)H^{2}+A^{2}H-A^{2} = 0,$$ de aquí $$Q-1+H = \frac{(1-H)A^{2}}{H^{2}}>0$$ reemplazando en $P_{1}(u)$ obtenemos $$P_{1}(u) = u^{2}+\frac{(1-H)A^{2}}{H^{2}}u+\frac{A^{2}}{H},$$ así nuevamente por la regla de los signos de Descartes, concluímos que $P_{1}$ no tiene raíces reales positivas, logrando con ello determinar que el polinomio \eqref{eq:polinomioIsoclinasModelo2} consta de una única raíz real positiva.
\end{proof}

\begin{remark}
	Con esta ultima proposición se concluye que el sistema \eqref{eq: modelo2} posee tres puntos de equilibrio biológicamente relevantes: $(0,0), \; (1,0) \text{ y } (H,H),$ donde, $H \in (0,1)$.
\end{remark}

%https://www-mathplanet-com.translate.goog/education/algebra-2/polynomial-functions/descartes-rule-of-sign?_x_tr_sl=en&_x_tr_tl=es&_x_tr_hl=es

Ahora, considerando $$g_1(u, v) = ((1 - u)(u^{2} + A^{2}) - Q u v)u^{2}=-Qu^3v-u^5+ u^4-A^2u^3+A^2u^2$$ y $$g_2(u, v) = B(u - v)(u^{2} + A^{2})v = Bu^3v-Bu^2v^2+A^2Buv-A^2Bv^2$$ la matriz jacobiana es:
\begin{equation*}
	Dg(Y) =
	\begin{bmatrix}
			-5u^{4} + 4u^{3} - (3A^{2} + 3Qv)u^{2} + 2A^{2}u & -Qu^{3} \\
			3Bu^{2}v+BvA^{2}-2Bv^{2}u & B(u^{2}+A^{2})(u-2v)
		\end{bmatrix}.
	\label{eq:jacobianaModelo2}
\end{equation*}

\subsection{Estabilidad del punto de equilibrio $(1,0)$}
En $(1,0)$:
\begin{equation*}
	Dg(1,0) =
	\begin{bmatrix}
		-(1+A^{2}) & -Q \\
		0 & B(1+A^{2})
	\end{bmatrix}.
	\label{eq:jacobianaModelo2En(1,0)}
\end{equation*}
Los valores propios son $\lambda_1 = -(1+A^{2}) < 0$ y $\lambda_2 = B(1+A^{2}) > 0$. Dado que tienen signos opuestos, $(1,0)$ es un punto silla.

\begin{figure}
	\centering
	\includegraphics[width=.5\textwidth]{img/puntoDeEquilibrio(1,0).pdf}
	\caption{Punto de equilibrio $(1, 0)$ para valores paramétricos aleatorios.}
	\label{fig: eq(1,0)Modelo2}
\end{figure}

\subsection{Estabilidad del punto de equilibrio $(0,0)$}
En $(0,0)$:
\begin{equation*}
	Dg(0,0) =
	\begin{bmatrix}
		0 & 0 \\
		0 & 0
	\end{bmatrix}.
	\label{eq:jacobianaModelo2En(0,0)}
\end{equation*}
En particular, para este punto de equilibrio al obtener la matriz nula, deducimos que el punto crítico $(0,0)$ es un punto no hiperbólico y degenerado, por lo que no es posible determinar la estabilidad de este punto por medio de la linealización ni el teorema de la variedad central, por ello, para analizar el comportamiento de este punto de equilibrio se recurrirá a desingularizarlo a través del método de blowing-up polar.

Para ello, escribamos el sistema \eqref{eq: modelo2} en coordenadas polares. Sustituyendo $u = r \cos\theta$ y $v = r \sin\theta$, obtenemos que $r^{2}=u^{2}+v^{2} \;\text{ y }\; \theta = \arctg\left(\dfrac{v}{u}\right)$ con $u\neq0$. Derivando con respecto al tiempo $\tau$: $$\dot{r}=\cos\theta\dot{u} + \sin\theta\dot{v} \;\text{ y }\; \dot{\theta}=\dfrac{\dot{v}\cos\theta-\dot{u}\sin\theta}{r}.$$ Reemplazando $\dot{u}$ y $\dot{v}$ obtenemos el sistema:
\begin{equation*}
	\begin{cases}
		\dot{r} &= r^{2} f(r, \theta)
		\\
		\dot{\theta} &= r g(r, \theta)
	\end{cases},
\end{equation*}
donde
\begin{equation*}
	\begin{aligned}
		f(r, \theta) &= -Qr^{2}\cos^{4}\theta\sin\theta- r^{3}\cos^{6}\theta+ r^{2}\cos^{5}\theta \\ &-A^{2}r\cos^{4}\theta+ A^{2}\cos^{3}\theta + Br^{2}\cos^{3}\theta\sin^{2}\theta \\ &-Br^{2}\cos^{2}\theta\sin^{3}\theta+ A^{2}B\cos\theta\sin^{2}\theta- A^{2}B\sin^{3}\theta
		\\
		g(r, \theta) &= Br^{2}\cos^{4}\theta\sin\theta- Br^{2}\cos^{3}\theta\sin^{2}\theta+ A^{2}B\cos^{2}\theta\sin\theta \\ &-A^{2}B\cos\theta\sin^{2}\theta+ Qr^{2}\cos^{3}\theta\sin^{2}\theta + r^{3}\cos^{5}\theta\sin\theta \\ &-r^{2}\cos^{4}\theta\sin\theta+ A^{2}r\cos^{3}\theta\sin\theta- A^{2}\cos^{2}\theta\sin\theta
	\end{aligned},
\end{equation*}
es decir, obtuvimos el campo vectorial $$\tilde{Y_{\eta}}(r,\theta) = r^{2}f(r,\theta) \partial_r + rg(r,\theta) \partial_\theta.$$ Dividiendo por $r$ obtenemos un nuevo campo $$\bar{Y_{\eta}}(r,\theta) = rf(r,\theta) \partial_r + g(r,\theta) \partial_\theta,$$ el cual, en $\{r=0\}$ es cualitativamente equivalente a una vecindad $V$ del origen del campo vectorial $Y_{\eta}$ dado por el sistema \eqref{eq: modelo2}, es decir, basta analizar el retrato fase del campo vectorial:
\begin{equation*}
	\begin{aligned}
		\bar{Y_{\eta}}(0,\theta) = g(0, \theta) &= A^{2}B\cos^{2}\theta\sin\theta - A^{2}B\cos\theta\sin^{2}\theta- A^{2}\cos^{2}\theta\sin\theta \\
		&= A^{2}\cos\theta\sin\theta (B\cos\theta-B\sin\theta-\cos\theta),
	\end{aligned}
\end{equation*}
para determinar el comportamiento del sistema \eqref{eq: modelo2} en una vecindad del origen.

Hallemos los puntos críticos de $\bar{Y_{\eta}}(0,\theta)$ ubicados en el primer cuadrante. Igualando el campo vectorial $\bar{Y_{\eta}}(0,\theta)$ a cero, debe ocurrir al menos que $\cos\theta$, $\sin\theta$ o $B\cos\theta-B\sin\theta-\cos\theta$ se anule. Esto ocurre para los valores $\theta_1 = 0, \; \theta_2 = \dfrac{\pi}{2}$ y los que cumplan la igualdad $$\dfrac{B-1}{B}\cos\theta=\sin\theta,$$ es decir, $\theta_3 = \arctg\left( \dfrac{B-1}{B} \right)$; aquí, notemos que el escenario de relevancia es para el caso $B>1$, pues para $B=1$ estaríamos hablando de $\theta_3 = \theta_1 = 0$ y para $B<1$, $\theta_3$ no estaría en el primer cuadrante.

Ahora, dada la matriz jacobiana:
\begin{equation*}
	D\bar{Y_{\eta}}(0,\theta) =
	\begin{bmatrix}
		h_{1}(\theta)  & 0\\
		A^{2}\cos^{3}\theta\sin\theta & h_{2}(\theta)
	\end{bmatrix},
	\label{eq:jacobianaModelo2Polar}
\end{equation*}
con
\begin{equation*}
	\begin{aligned}
		h_{1}(\theta) &= A^{2}\cos^{3}\theta + A^{2}B[\cos\theta\sin^{2}\theta - \sin^{3}\theta] \\
		h_{2}(\theta) &= A^{2}B[\sin^{3}\theta + \cos^{3}\theta - 2\sin^{2}\theta\cos\theta - 2\sin\theta\cos^{2}\theta] \\
		& + A^{2}[2\cos\theta\sin^{2}\theta - \cos^{3}\theta].
	\end{aligned}
\end{equation*}

\subsubsection*{Estabilidad en $(0, \theta_1)$}
En el punto crítico $(0,0)$, obtenemos la matriz:
\begin{equation*}
	D\bar{Y_{\eta}}(0,0) =
	\begin{bmatrix}
		A^{2}  & 0 \\
		0 & A^{2}(B-1)
	\end{bmatrix},
	\label{eq:jacobianaModelo2PolarEn(0,0)}
\end{equation*}
con valores propios $\lambda_r = A^{2}>0$ y $\lambda_\theta = A^{2}(B-1)$. Por lo tanto, $(0, \theta_1)$ es un punto silla de $\bar{Y_{\eta}}(0,\theta)$ si $B<1$, es una fuente si $B>1$ y un punto crítico no hiperbólico si $B=1$. Aquí observamos qué la estabilidad de este punto de equilibrio depende del parámetro $B$, la cual influirá en la estructura cualitativa del campo $\bar{Y_{\eta}}(r,\theta)$ y por ende en la del sistema \eqref{eq: modelo2}. % Así, obtenemos que hay un cambio cualitativo en el valor de bifurcación $B=1$, posteriormente se analizará que tipo de bifurcación se presenta en este modelo.

Ahora, debido a que en el caso $B=1$ el punto $(0,\theta_1)$ es un punto no hiperbólico, no es posible aplicar el teorema de Harmant-Grobman para determinar su estabilidad y por ello recurriremos al teorema de la variedad central.

Recordemos que este último nos garantiza la existencia de una curva invariante $r = h(\theta)$ que define la variedad central en una vecindad del origen, cuyo flujo esta determinado por la ecuación diferencial $\dot{\theta} = g(h(\theta), \theta)$. En nuestro caso, al estar determinando la estabilidad de los puntos críticos en el circulo ${r = 0}$, esto conlleva a la omisión de tener que especificar como esta definida la curva invariante $h(\theta)$. Por ende, para precisar la estabilidad del origen para el caso $B = 1$, solo basta determinar el flujo de la ecuación diferencial $\dot{\theta} = g(0, \theta) = - A^2 \sin ^2 \theta \cos \theta$; para ello, hallemos los primeros términos de la expansión en serie de Taylor de $-A^2 \sin ^2 \theta \cos \theta$ alrededor de $\theta = 0$. Como

\begin{equation*}
	\begin{aligned}
		\sin \theta &= \theta - \dfrac{\theta^3}{3!} + \mathcal{O}(\theta^ 5) \\
		\sin^2 \theta &= \theta^2 + \mathcal{O}(\theta^ 4) \\
		\cos \theta &= 1 - \dfrac{\theta^2}{2!} + \mathcal{O}(\theta^ 4)
	\end{aligned},
\end{equation*}
tenemos que
\begin{equation*}
	\dot{\theta} = - A^2 \sin ^2 \theta \cos \theta = -A^2 \theta^2 + \mathcal{O}(\theta^ 4).
\end{equation*}
Así, como el orden del primer termino de la expansión es par, se concluye que $(0, \theta_1)$ para el caso $B=1$ es un punto silla-nodo de $\bar{Y_{\eta}}(0,\theta)$ \cite{perko2001}[p.~157].

\subsubsection*{Estabilidad en $(0, \theta_2)$}

Tenemos que:
\begin{equation*}
	D\bar{Y_{\eta}}\left(0,\dfrac{\pi}{2}\right) =
	\begin{bmatrix}
			-A^{2}B  & 0 \\
			0 & A^{2}B
		\end{bmatrix},
	\label{eq:jacobianaModelo2PolarEn(0,pi2)}
\end{equation*}
con valores propios $\lambda_r = -A^{2}B<0$ y $\lambda_\theta = A^{2}B>0$. Por lo tanto, $\left(0,\theta_2\right)$ es un punto silla de $\bar{Y_{\eta}}(0,\theta)$.

\subsubsection*{Estabilidad en $(0, \theta_3)$}

Ahora, denotando $b = \left( \dfrac{B-1}{B} \right)$, donde $B>1$, y tomando en cuenta las identidades
\begin{equation*}
	\begin{aligned}
		\sin(\arctg(b)) &= \dfrac{b}{\sqrt{1+b^{2}}} \\
		\cos(\arctg(b)) &= \dfrac{1}{\sqrt{1+b^{2}}}
	\end{aligned},
\end{equation*}
llegamos a que
\begin{equation*}
	D\bar{Y_{\eta}}\left(0,\arctg(b)\right) = \dfrac{A^{2}}{(1+b^{2})^{3/2}}
	\begin{bmatrix}
		a_{11} & 0 \\
		\dfrac{b}{(1+b^{2})^{1/2}} & a_{22}
	\end{bmatrix},
\end{equation*}
donde
\begin{equation*}
	\begin{aligned}
		a_{11} &= 1 + Bb^{2}(1-b) \\
		a_{22} &= B(b^{3}-2b+1) -2b^{2}(B-1) - 1
	\end{aligned}
\end{equation*}
reemplazando $b = \left( \dfrac{B-1}{B} \right)$, obtenemos que:
\begin{equation*}
	\begin{aligned}
		a_{11} &= 1 + B\dfrac{(B-1)^{2}}{B^{2}}\left(1-\dfrac{B-1}{B}\right) \\ 
		&=\dfrac{2B^{2}-2B+1}{B^{2}} \\
		a_{22} &= B\left(\dfrac{(B-1)^{3}}{B^{3}}-2\dfrac{B-1}{B}+1\right) -2\dfrac{(B-1)^{2}}{B^{2}}(B-1) - 1 \\ 
		&= 1 - B - \dfrac{(B-1)^{3}}{B^{2}} \\
		1+b^{2} &= \dfrac{2B^{2}-2B+1}{B^{2}}
	\end{aligned}
\end{equation*}
Así,
\begin{equation*}
	D\bar{Y_{\eta}}\left(0,\arctg(b)\right) = \alpha
	\begin{bmatrix}
		\dfrac{2B^{2}-2B+1}{B^2} & 0 \\
		\dfrac{(B-1)B}{(2B^{2}-2B+1)^{1/2}} & -\dfrac{(B-1)(2B^{2}-2B+1)}{B}
	\end{bmatrix},
	\label{eq:jacobianaModelo2PolarEn(0,b)}
\end{equation*}
donde $\alpha = \dfrac{A^{2}B^{3}}{(2B^{2}-2B+1)^{3/2}}>0$. Con valores propios $$\lambda_r =\alpha \dfrac{2B^{2}-2B+1}{B^{2}}>0 \; \text{y} \; \lambda_\theta  = -\alpha \dfrac{(B-1)(2B^{2}-2B+1)}{B}<0.$$ Por lo tanto, $(0,\theta_3)$ es un punto silla.
\begin{figure}
	\centering
	\includegraphics[width=.7\textwidth]{img/blowingUpPolar.pdf}
	\caption{Blow-up en el origen del sistema \eqref{eq: modelo2} para el caso $B>1$.}
	\label{fig: BlowUp Modelo 2}
\end{figure}

Luego de hallar la estabilidad de cada punto crítico de $\bar{Y_{\eta}}$, junto con sus respectivos valores propios radiales y tangenciales, podemos determinar como se comporta cualitativamente el origen del campo $Y_{\eta}$ en una cierta vecindad. Este comportamiento se puede evidenciar en la figura \ref{fig: BlowUp Modelo 2}, en donde, para el caso $B\leq1$ vemos que el primer cuadrante consta de un único sector hiperbólico, y para $B>1$ de uno parabólico y uno hiperbólico. Recordemos que la finalidad de la técnica del blow-up es determinar el comportamiento del origen en $Y_{\eta}$, en sí, este proceso nos permite determinar que trayectorias entran o salen del origen, y son determinadas por la pendiente $\tan \theta^*$, donde $\theta^*$ es un punto crítico de $\bar{Y_{\eta}}$. Por ende, en el primer cuadrante de $Y_{\eta}$ hay tres de estas trayectorias: $v=0$ que corresponde a la orbita con pendiente $\tan\theta_1=0$, $u=0$ que corresponde a la orbita con pendiente $\tan\theta_2= \infty$ y la orbita determinada por la pendiente $\tan\theta_3=\dfrac{B-1}{B}$, la cual, esta última solo se da para el caso $B>1$, y además define una curva separatriz puesto que divide el retrato de fase en dos sectores con comportamientos cualitativos distintos (parabólico e hiperbólico).

\begin{figure}
	\centering
	\begin{subfigure}{0.49\textwidth}
		\includegraphics[width=\textwidth]{img/puntoDeEquilibrio(0,0)conBmenor1.pdf}
		\caption{Retrato fase del sistema \eqref{eq: modelo2} cerca al origen para valores paramétricos aleatorios con $B < 1$.}
		\label{fig: eq(0,0)Modelo2Bmenor1}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.49\textwidth}
		\includegraphics[width=\textwidth]{img/puntoDeEquilibrio(0,0)conBmayor1.pdf}
		\caption{Retrato fase del sistema \eqref{eq: modelo2} cerca al origen para valores paramétricos aleatorios con $B > 1$.}
		\label{fig: eq(0,0)Modelo2Bmayor1}
	\end{subfigure}
	\caption{Retrato fase del sistema \eqref{eq: modelo2} cerca al origen.}
	\label{fig: eq(0,0)Modelo2}
\end{figure}

\subsection{Estabilidad del punto de equilibrio $(H,H)$}
\label{sec: HH}
Con el fin de reducir los posibles casos que se pueden dar en los valores propios, reduciremos la cantidad de parámetros realizando la sustitución
\begin{equation}
	Q=\dfrac{(1-H)(A^2+H^{2})}{H^{2}},
	\label{eq: Q}
\end{equation}
anteriormente discutida en la \Cref{pro:exitenciaH}. Así, tendríamos que en $(H,H)$:
\begin{equation}
	Dg(H,H) =
	\begin{bmatrix}
		-H(H^{2}(2H-1)+A^2) & -H(1-H)(A^2+H^2) \\
		BH(A^{2}+H^{2}) & -BH(A^{2}+H^{2})
	\end{bmatrix}.
	\label{eq:jacobianaModelo2En(H,H)}
\end{equation}
Los valores propios se hallan del polinomio característico $\lambda^2 - \operatorname{tr}(M) \lambda + \det(M) = 0$, con $M = Dg(H,H)$, estos es:
\begin{equation*}
	\lambda(\mu) = \dfrac{\operatorname{tr}(M) \pm \sqrt{\Delta}}{2},
\end{equation*}
donde $\mu = (A, \; B)$, $\Delta = (\operatorname{tr}(M))^2 - 4\det(M)$ y
\begin{equation*}
	\begin{aligned}
		\operatorname{tr}(M) &= -H(B(A^{2}+H^{2})+H^{2}(2H-1)+A^2) \\
		\det(M) &= BH^2(A^2+H^2)(H^3+A^2(2-H))
	\end{aligned}.
\end{equation*}
Como $H \in (0, 1)$, $\det(M)>0$ por lo que $(H,H)$ es un punto no degenerado e hiperbólico o un centro lineal; además, no puede ser un punto silla. Así la estabilidad estaría determinada por el signo de la traza y del discriminante $\Delta$, para ello notemos que $\operatorname{tr}(M)=0$ si
\begin{equation}
	B = \dfrac{H^2(1-2H) - A^2}{A^2 + H^2} := B_c,
	\label{eq: Bcritico}
\end{equation}
y puesto que $B>0$, esto se da bajo las restricciones: $H<1/2$ y $A^2 < H^2(1-2H)$. Este hecho da paso a los siguientes casos:
\begin{itemize}
	\item Si $B<B_c$ y
	\subitem $\Delta < 0$, $(H,H)$ es un foco inestable.
	\subitem $\Delta = 0$, $(H,H)$ es un nodo degenerado inestable.
	\subitem $\Delta > 0$, $(H,H)$ es un nodo inestable.
	\item Si $B=B_c$, $(H,H)$ es un centro lineal.
	\item Si $B>B_c$ y
	\subitem $\Delta < 0$, $(H,H)$ es un foco estable.
	\subitem $\Delta = 0$, $(H,H)$ es un nodo degenerado estable.
	\subitem $\Delta > 0$, $(H,H)$ es un nodo estable.
\end{itemize}				

Dado que la linealización no garantiza que $(H, H)$ sea un punto centro para el caso $B=B_c$, ya que en este caso el punto de equilibrio es no hiperbólico, se es de importancia verificar si $(H, H)$ es un punto centro o un foco débil en el sistema no linealizado. Con el propósito de validar la estructura cualitativa en este caso, calcularemos el número de Lyapunov; esto no es sólo con este fin, sino con el de validar que tipo de bifurcación de Hopf se nos presenta, puesto que dicha característica esta garantizada debido al siguiente teorema.

\begin{teo}
	Una bifurcación de Hopf ocurre en el equilibrio $(H, H)$ para el valor de bifurcación $B = B_c$.
\end{teo}
\begin{proof}
    Para que ocurra una bifurcación de Hopf se deben satisfacer la condición espectral y la de transversalidad. Por lo anteriormente planteado, el Jacobiano de la función evaluado en el equilibrio $(H, H)$ para $B = B_c$, posee un par de autovalores imaginarios puros $\lambda_1, \; \lambda_2 = \pm \omega i$, donde $\omega = \sqrt{\det (M)} > 0$, satisfaciendo así la condición espectral. Por otra parte, la velocidad de cruce de la parte real de los autovalores respecto al parámetro de bifurcación $B$, debe ser distinta de cero en el punto crítico:
    \begin{equation*}
    	\begin{aligned}
    		\left. \frac{d}{dB}(Re(\lambda(\mu))) \right|_{B=B_c} &= \left. \frac{d}{dB}\left(\frac{\operatorname{tr}(M)}{2}\right) \right|_{B=B_c} \\
    		&= -\frac{1}{2}H(A^{2}+H^{2}) \neq 0
    	\end{aligned}
    \end{equation*}
    Cumpliendo así las condiciones para que una bifurcación de Hopf ocurra para $B = B_c$.
\end{proof}

\begin{figure}
	\centering
	\includegraphics[width=\textwidth]{img/puntoDeEquilibrio(H,H).pdf}
	\caption{Punto de equilibrio $(H, H)$ para diferentes valores paramétricos.}
	\label{fig: eq(H,H)Modelo2}
\end{figure}

\subsubsection{Calculo del primer coeficiente de Lyapunov}
Para proceder con el calculo del primer coeficiente de Lyapunov, debemos trasladar el punto de equilibrio $(H, H)$ al origen. Para ello, realicemos las sustituciones $u = \bar{u} + H$ y $v = \bar{v} + H$ en el sistema \eqref{eq: modelo2} y reemplazamos el valor crítico $B_c$ con el fin de obtener $(H,H)$ como un centro lineal (Valores propios complejos puros en la matriz jacobiana del sistema trasladado en el punto $(H,H)$). Estos cambios se ven reflejados de la siguiente manera:
\begin{equation}
	W_{\eta}:
	\begin{cases}
		\dfrac{d\bar{u}}{d \tau} = h_1(u, v)\\
		\dfrac{d\bar{v}}{d \tau} = h_2(u, v)
	\end{cases},
	\label{eq: modelo2Hen0}
\end{equation}
donde,
\begin{equation*}
	\begin{aligned}
		h_1(\bar{u}, \bar{v}) &= [(1 - \bar{u} - H)[(\bar{u} + H)^{2} + A^{2}] - Q (\bar{u} + H) (\bar{v} + H)](\bar{u} + H)^{2} \\
		h_2(\bar{u}, \bar{v}) &= B_c(\bar{u} - \bar{v})[(\bar{u} + H)^{2} + A^{2}](\bar{v} + H)
	\end{aligned}
\end{equation*}
Por la igualdad \eqref{eq: Q}, la matriz jacobiana del sistema \eqref{eq: modelo2Hen0} en el origen estaría dada simplemente por la matriz \eqref{eq:jacobianaModelo2En(H,H)} sustituyendo $B=B_c$:
\begin{equation*}
	Dh(0,0) =
	\begin{bmatrix}
		H[H^{2}(1-2H)-A^2] & -H[(1-H)(A^2+H^2)] \\
		H[H^2(1-2H) - A^2] & - H[H^2(1-2H) - A^2]
	\end{bmatrix} :=
	\begin{bmatrix}
	a & b \\
	a & - a
	\end{bmatrix}.
	\label{eq:jacobianamodelo2Hen0}
\end{equation*}
Ahora debemos transformar el sistema a su forma normal:
\begin{equation}
	\begin{cases}
		\dot{X_1} = -\omega X_2 + G_1(X_1, X_2) \\
		\dot{X_2} = \omega X_1 + G_2(X_1, X_2)
	\end{cases},
	\label{eq: normalForm}
\end{equation}
donde $\omega = \sqrt{\det (M)}$. Para ello debemos hallar un autovector asociado al valor propio complejo puro positivo de la matriz jacobiana $Dh(0,0)$: $$\omega i= \sqrt{\det (M)} \; i = H\sqrt{(H^3+A^2(2-H))(H^2(1-2H) - A^2)} \; i := H \sqrt{mn} \; i.$$ Esto es equivalente a solucionar el sistema, $(Dh(0, 0) - \omega i I)U = 0$, esto es:
\begin{equation}
	\begin{cases}
	    (a - \omega i)U_1 + b U_2 = 0\\
		a U_1 - (a + \omega i)U_2 = 0
	\end{cases},
	\label{eq: VectoresPropiosOmega}
\end{equation}
De aquí $U_1 = [1 + (a+b)/\omega i]U_2$. Tomando $U_2 = 1$, una posible solución es el vector:
\begin{equation}
	\begin{pmatrix}
		1 \\
		1 
	\end{pmatrix} +
	\begin{pmatrix}
		\sqrt{\dfrac{m}{n}} \\
		0
	\end{pmatrix} i
	:=
	\begin{pmatrix}
		1 \\
		1 
	\end{pmatrix} +
	\begin{pmatrix}
		d\\
		0
	\end{pmatrix} i
\end{equation}
Así, nuestra matriz de transformación esta dada por
\begin{equation*}
	P =
	\begin{pmatrix}
		d && 1 \\
		0 && 1
	\end{pmatrix}\; \; \; \; \text{con inversa} \; \; \; \;
		P^{-1} = \dfrac{1}{d}
	\begin{pmatrix}
		1 && -1 \\
		0 && d
	\end{pmatrix}.
\end{equation*}
Ahora, con el fin de identificar la parte no lineal de nuestro sistema, realizamos $(h_1(\bar{u}, \bar{v}), h_2(\bar{u}, \bar{v}))^{T} - Dh(0,0)(\bar{u}, \bar{v})^{T}$. La cual resulta estar compuesta por:
\begin{equation}
	\begin{aligned}
		F_1(\bar{u}, \bar{v}) = h_1(\bar{u}, \bar{v}) - a\bar{u} - b\bar{v}\\
		F_2(\bar{u}, \bar{v}) = h_2(\bar{u}, \bar{v}) - a\bar{u} + a\bar{v}
	\end{aligned}
\end{equation}
Así el sistema \eqref{eq: modelo2Hen0} pasa a ser:
\begin{equation}
	W_{\eta}:
	\begin{cases}
		\dfrac{d\bar{u}}{d \tau} = a\bar{u} + b\bar{v} + F_1(\bar{u}, \bar{v})\\
		\dfrac{d\bar{v}}{d \tau} = a\bar{u} - a\bar{v} + F_2(\bar{u}, \bar{v})
	\end{cases},
\end{equation}
Dado que el sistema aún no posee la simetría esperada, debemos realizar la sustitución $(\bar{u}, \bar{v})^{T} = P \cdot (X, Y)^{T}$. El sistema transformado estará dado por $(\dot{X}, \dot{Y})^T = P^{-1}Dh(0,0)P(X, Y)^{T}+P^{-1}F(P(X, Y)^{T})$, es decir:
\begin{equation}
	\begin{cases}
		\dot{X} = -\omega Y + G_1(X, Y) \\
		\dot{Y} = \omega X + G_2(X, Y)
	\end{cases}
	\label{eq: normalFormWn}
\end{equation}
donde
\begin{equation*}
	\begin{aligned}
		G_1(X, Y) &= \dfrac{1}{d}[F_1(dX + Y, Y) -F_2(dX + Y, Y)] \\
		&= \dfrac{1}{d}[h_1(dX + Y, Y) - h_2(dX + Y, Y) - (a+b)Y] \\
		G_2(X, Y) &= F_2(dX + Y, Y) \\
		&= h_2(dX + Y, Y) - adX
	\end{aligned}
\end{equation*}
contienen los terminos de orden superior del sistema. La expansión de Taylor de orden 3 de estas expresiones, estarán dadas por
\begin{equation*}
	\begin{aligned}
		G_1(X, Y) &= X [\left((3-B_c) A^2+6 m+13 n-H^2 (3 B_c+9 Q+1)\right) Y \\
		&-3 \left(A^2+H (B_c+10 H+3 Q-4)\right) Y^2 \\
		&+(-B_c-20 H-3 Q+4) Y^3+\mathcal{O}\left(Y^4\right)] \\
		&+X^2 [\frac{1}{2} d \left(3 A^2+6 m+13 n-H^2 (4 B_c+6 Q+1)\right) \\
		&-d \left(3 A^2+4 B_c H+6 H (5 H+Q-2)\right) Y \\
		&-d (2 B_c+30 H+3 Q-6) Y^2-10 d Y^3+\mathcal{O}\left(Y^4\right)] \\
		&+X^3 [-d^2 \left(A^2+H (B_c+10 H+Q-4)\right)-d^2 (B_c+20 H+Q-4) Y \\ 
		&-10 d^2 Y^2+\mathcal{O}\left(Y^4\right)]+\mathcal{O}\left(X^4\right)\\
		&+ \dfrac{1}{d}[\frac{\left(3 A^2+H^2 (-12 Q-1)+6 m+13 n\right)}{2} Y^2 \\ 
		&- \left(A^2+2 H (5 H+2 Q-2)\right)Y^3+ \mathcal{O} (Y^4)] \\
		\\
		G_2(X, Y) &= X \left[B_c d \left(A^2+3 H^2\right) Y+3 B_c d H Y^2+B_c d Y^3+\mathcal{O}\left(Y^4\right)\right] \\
		&+X^2 \left[2 B_c d^2 H^2+4 B_c d^2 H Y+2 B_c d^2 Y^2+\mathcal{O}\left(Y^4\right)\right] \\
		&+X^3 \left[B_c d^3 H+B_c d^3 Y+\mathcal{O}\left(Y^4\right)\right]+\mathcal{O}\left(X^4\right)
	\end{aligned}
\end{equation*}
El primer coeficiente de Lyapunov estará dado por
\begin{equation*}
	\begin{aligned}
		L_1 &= \frac{1}{16} [ G_{1,xxx} + G_{1,xyy} + G_{2,xxy} + G_{2,yyy} \\
		&+ \frac{1}{\omega} ( G_{1,xy}(G_{1,xx} + G_{1,yy}) - G_{2,xy}(G_{2,xx} + G_{2,yy}) \\
		&- G_{1,xx}G_{2,xx} + G_{1,yy}G_{2,yy} )]
	\end{aligned}
\end{equation*}
donde cada derivada parcial está evaluada en el origen, las cuales están dadas por:
\begin{equation*}
	\begin{aligned}
		G_{1,XX} &= d \left(3 A^2-H^2 (4 B_c+6 Q+1)+6 m+13 n\right) \\
		G_{1,YY} &= \frac{1}{d}\left(3 A^2+H^2 (-12 Q-1)+6 m+13 n\right) \\
		G_{1,XY} &= A^2 (3-B_c)-H^2 (3 B_c+9 Q+1)+6 m+13 n \\
		G_{1,XXX} &= -6 d^2 \left(A^2+H (B_c+10 H+Q-4)\right) \\
		G_{1,XYY} &= -6 \left(A^2+H (B_c+10 H+3 Q-4)\right) \\ 
		\\
		G_{2,XX} &= 4 B_c d^2 H^2 \\
		G_{2,YY} &= 0 \\
		G_{2,XY} &= B_c d \left(A^2+3 H^2\right) \\
		G_{2,XXY} &= 8 B_c d^2 H \\
		G_{2,YYY} &= 0
	\end{aligned}
\end{equation*}
Reemplazando los valores conocidos $d, \; m, \; n, \; B_c$ y $Q$, tenemos que $$L_1 = \dfrac{A^2 H(1 - H)([4 - 3H(3 - H)]A^4 -4H^3A^2 - (3 - H)H^5)}{2(A^2 + H^2)(H^3 + A^2(2 - H))(H^2(1 - 2H)- A^2)},$$ donde el signo de $L_1$ dependerá exclusivamente del valor
\begin{equation}
	[4 - 3H(3-H)]A^4 - 4H^3A^2 - (3-H)H^5,
	\label{eq: sgnL1}
\end{equation}
con $4 - 3H(3-H) > 0$ puesto que $H \in (0, 1/2)$. El primer coeficiente de Lyapunov se anulará para:
\begin{equation*}
	\begin{aligned}
		A^2 &= \dfrac{4H^3 \pm \sqrt{16 H^6 + 4[4 - 3H(3-H)] (3-H)H^5}}{2 [4-3H(3-H)]} \\
		&= \dfrac{2H^3 + H^2(1 - H)\sqrt{3H(4 - H)}}{4-3H(3-H)} := A^2_c
	\end{aligned}
	\label{eq: parametroBifHopfGeneralizada}
\end{equation*}
La curva $L_1 = 0$ y la región de restricciones del sistema, puede observarse en la \autoref{fig: curvaL_1AH}. El modelo contará con una bifurcación subcrítica ($L_1 > 0$) y un ciclo límite inestable para los valores de $A$ y $H$ que se encuentren por encima de esta curva y dentro de la región restrictiva, de manera análoga el sistema poseerá una bifurcación supercrítica con un ciclo límite estable para los valores que se encuentren debajo de esta curva y dentro de la región dada por las restricciones del sistema. El caso $L_1 > 0$, es de vital importancia debido a las características del sistema, como la región $\Gamma$ es invariante, y los demás puntos críticos no poseen propiedades atractoras dentro de la región, las trayectorias fuera del ciclo límite inestable deben tender a otro ciclo limite estable según el teorema de Poincaré-Bendixson, esto garantiza la existencia de un ciclo límite adicional en este caso.
\begin{figure}
	\centering
	\includegraphics[width=.8\textwidth]{img/CurvaLyapunovAH.pdf}
	\caption{Curva $L_1 = 0$ y la región dada por las restricciones $H<1/2$ y $A^2 < H(1-2H)$.}
	\label{fig: curvaL_1AH}
\end{figure}
\begin{figure}
	\centering
	\begin{subfigure}{0.49\textwidth}
		\includegraphics[width=\textwidth]{img/HopfSuperCentrolineal.pdf}
		\caption{Bifurcación de Hopf supercrítica ($L_1 = -0.0250864$) del sistema \eqref{eq: modelo2} para los valores $A = 0.05; \; H = 0.4; \; B=B_c=0.181538$ y $Q=0.609375$ con el punto crítico de estudio como un foco débil inestable.}
		\label{fig: bifurcacionHopfsup}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.49\textwidth}
		\includegraphics[width=\textwidth]{img/HopfSubCentrolineal.pdf}
		\caption{Bifurcación de Hopf subcrítica ($L_1 = 0.0589$) del sistema \eqref{eq: modelo2} para los valores $A = 0.05; \; H = 0.06; \; B=B_c=0.109508$ y $Q=1.59278$ con el punto crítico de estudio como un foco débil estable.}
		\label{fig: bifurcacionHopfsub}
	\end{subfigure}
	\caption{Bifurcación de Hopf del sistema \eqref{eq: modelo2}.}
	\label{fig: bifurcacionHopf}
\end{figure}

Ahora, nótese que para el valor paramétrico \eqref{eq: parametroBifHopfGeneralizada}, el primer coeficiente de lyapunov se anula, teniendo como consecuencia no poder definir la estabilidad del punto de equilibrio $(H, H)$, en este caso. Para poder determinarla debemos proceder a calcular el segundo coeficiente de lyapunov. Esto además nos permitirá determinar que tipo de bifurcación de Bautin se nos presenta.

\begin{teo}
	Una bifurcación de Bautín (Hopf generalizada) ocurre en el equilibrio $(H, H)$ para los valores de bifurcación $\mu_0 = (A_c, B_c)$.
\end{teo}
\begin{proof}
	Este hecho se da simplemente del caso de que el sistema posee valores propios complejos puros para $B=B_c$, y de que $L_1 = 0$ para $A = A_c$. Donde
	\begin{equation}
		B_c = \frac{2-3H - \sqrt{3H(4 - H)}}{2}
	\end{equation}
	bajo la restricción $H < (3 - \sqrt{6})/3 \approx 0.1835$ que serían los valores para las cuales se satisfacen las restricciones $H<1/2$, $A^2 < H^2(1-2H)$ planteadas en la \autoref{sec: HH} al sustituir $A = A_c$.
\end{proof}

\subsubsection{Calculo del segundo coeficiente de Lyapunov}
Procedemos a reemplazar el valor paramétrico $A=A_c$ en el sistema \eqref{eq: modelo2Hen0} con el fin de se satisfagan las condiciones necesarias para la bifurcación de Bautin.
\begin{equation}
	Z_{\eta}:
	\begin{cases}
		\dfrac{d\bar{u}}{d \tau} = \bar{h_1}(u, v)\\
		\dfrac{d\bar{v}}{d \tau} = \bar{h_2}(u, v)
	\end{cases},
	\label{eq: modelo2Hen0Ac}
\end{equation}
donde,
\begin{equation*}
	\begin{aligned}
		\bar{h_1}(\bar{u}, \bar{v}) &= [(1 - \bar{u} - H)[(\bar{u} + H)^{2} + A_c^{2}] - Q (\bar{u} + H) (\bar{v} + H)](\bar{u} + H)^{2} \\
		\bar{h_2}(\bar{u}, \bar{v}) &= B_c(\bar{u} - \bar{v})[(\bar{u} + H)^{2} + A_c^{2}](\bar{v} + H)
	\end{aligned}
\end{equation*}